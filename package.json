{
  "name": "tolk",
  "private": true,
  "workspaces": ["api", "frontend", "shared"],
  "scripts": {
    "start": "bun install && bun run --cwd shared build && concurrently -n shared,api,frontend -c gray,blue,magenta \"bun run --cwd shared dev\" \"bun run --cwd api dev\" \"bun run --cwd frontend dev\"",
    "build": "bun install && bun run --cwd shared build && bun run --cwd api build && bun run --cwd frontend build"
  },
  "devDependencies": {
    "@ryoppippi/unplugin-typia": "^2.6.5",
    "@types/express-winston": "^4.0.2",
    "@types/pg": "^8.16.0",
    "api": "^6.1.3",
    "concurrently": "^9.2.1",
    "express-winston": "^4.2.0",
    "ts-patch": "^3.3.0",
    "winston": "^3.19.0"
  }
}
